# Netlify (build): CMS for SSG and media URLs
UMBRACO_API_URL=https://localhost:44392
UMBRACO_API_TOKEN=5df43bf5-46ca-4976-8087-f3972df5849b

# Optional: GitHub PAT for speaker activity (higher rate limit at build/dev). Server-only — never PUBLIC_.
# GITHUB_TOKEN=ghp_xxxxxxxx

# Netlify (build or deploy): form endpoints (optional; defaults to uiug2025.s.cmshelp.dk when not DEV)
PUBLIC_SUBSCRIBE_API_URL=https://localhost:44392/api/subscribe/submit
PUBLIC_CONTACT_API_URL=https://localhost:44392/api/contact/submit
